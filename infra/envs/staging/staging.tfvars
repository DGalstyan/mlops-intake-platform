environment = "staging"
region      = "us-east-1"
project     = "intake"

# Update if the repo is forked/renamed — this scopes the ci-deploy OIDC trust
# condition, it is not a secret.
github_repository = "DGalstyan/mlops-intake-platform"
